- Hosting and publicly accessible
	- basic auth
	- tls
	- reverse proxy

- Add some simple prompting integration, like add suggestions as a mentor.
	- Need to have project summaries for this, ideally.
- Idempotency of database init.
- Add local DM Sans font files and wire up @font-face in CSS (replace external Google Fonts import).
- Maybe add checkboxes for the list items?
- Add tracking of updated times so that we can sort projects by last touched.
- add load testing suite docs/plans/load_testing_step1_solution_assessment.md
- Basic auth
	- Logging of actions
	- Logging of who did what actions

Feedback from Daniel
- Resident rather than project x
- Make the slider go from 0 to whatever the end value is (e.g. 100 MRR) x
- List items: log the date it was logged (Residency runs for 12 weeks) x
- Put questions right below project x
- Add resident summary x
- North star graph (log datapoints) x
- Allow arranging things x
- Confetti and other cheerful things x

transcripts
	- store transcript in database for later review
		- use supabase for storing
		- convert to mp3 
	- allow recording of session in-browser
		- show audio indicator
		- transcribe session
	- glue it all together into a nice ui/process

Feedback from Daniel:
- getting live recording from a web app could be challenging
	- do a longer recording test
	- will the processing take longer too?
	- does it take longer than 45 seconds? too long
	- live recording: 2 versions
		- play a video from a speaker
		- use a vpn to access the website from somewhere far from us-east
		- network throttling test

stakeHolders:
	theresidency
	residents
	architects
	coaches

user profiles
	- linked to community

click here and show all the questions to ask

import the current records

auth
	add email records
	allow auth by email

Interview Guide UI Improvements
	- Split view on desktop, drawer on mobile.
	- Section checklist during interview with asked-count progress.
	- Search/filter within the guide prompts.
	- Inline quick actions per question (copy, insert into transcript).
	- Persist guide state per resident/session.
	- Accessibility polish (focus order, keyboard shortcuts, ARIA live feedback).


MVP beta
	- Indicate current user x
	- Get rid of lobby x
	- No settings x
	- No residents manager x
	- Paste or upload, no recorder x
	- Test with long transcript

Env defaults sync follow-up
	- Problem statement: launch-time `.env` syncing currently imports commented `.env.example` example lines as real values (for example `DATABASE_URL=sqlite:///data/north_star.db`), which can create invalid/unintended runtime config and startup failures (like PermissionError on `/data`).

- Import data in sqlite file

Roadmap:
	- Pitch to Penelope, and it gives you feedback

lobby_badge_realtime_step1_solution_assessment.md

bug
	Unset=> all optional nav items enabled. x
	in .env still added by env-sync x

assign each community leader to a house, and remove house selector x

re-check the file upload x

show confetti after the upload is added x
	instead of when the north start status is updated x

are original values included in the prompting? x
	yes x

get rid of [import] notes in questions x

keep the functionality of the questions x

generate the questions field x
	questions for follow-ups x

resident summary and questions should not be editable x

why do the boxes have all the same dates? x
	follow up by manually checking
	ensure all updates are imported, not just latest

when generating a magic link, create a placeholder user right away, so that we can assign to a house x